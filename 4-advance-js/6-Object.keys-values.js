const person = {
    name: 'xx',
    age: 12
}
// - return as array
const keys = Object.keys(person) // keys 
const values = Object.values(person) //values
const entries = Object.entries(person) // both

console.log(keys, values, entries);